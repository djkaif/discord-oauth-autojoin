# Discord OAuth2 Auto-Join System

This repository allows users to click a button, authorize your Discord app, and automatically join your Discord server using the `guilds.join` OAuth2 scope.

## Requirements
- Bot must already be in the target server
- Bot must have "Manage Server" or invite perms
- You need a domain with HTTPS for production

## Setup

### 1. Install dependencies
